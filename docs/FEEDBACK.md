# Feedback Intake

Use GitHub issue template `.github/ISSUE_TEMPLATE/feedback.yml`.
Triage weekly via Phase 4 milestone.

Labels:
- `version` for schema/breaking changes
- `feedback` for user-reported issues
- `templates` for scaffold requests
